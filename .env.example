# Supabase Configuration (Optional)
# The app works offline with localStorage if these are not configured.
# To enable cloud sync and authentication:
# 1. Create a Supabase project at https://supabase.com
# 2. Copy your project URL and anon key from Settings > API
# 3. Rename this file to .env.local and fill in your values

VITE_SUPABASE_URL=your-project-url.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Note: For Vercel deployment, add these as environment variables in your project settings
